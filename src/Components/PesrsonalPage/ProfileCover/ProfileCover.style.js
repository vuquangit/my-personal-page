import {makeStyles} from '@material-ui/core/styles';

export const ProfileCoverStyle = makeStyles((theme) => ({
  root: {
    flexGrow: 1,
    // height: '40vh',
    width: '100%',
  },
  sliderItem: {
    height: '100%',
    width: '100%',
  },
}));
